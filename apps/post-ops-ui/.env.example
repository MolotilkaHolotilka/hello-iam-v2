# Copy to `.env` in this directory (same folder as package.json):
#   cp .env.example .env
# Then set FAL_KEY from https://fal.ai/dashboard (API keys).
#
# Never commit `.env`.

FAL_KEY=
FAL_MODEL=fal-ai/flux/schnell
